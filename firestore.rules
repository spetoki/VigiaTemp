rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // AVISO: Regra insegura, apenas para desenvolvimento.
    // Permite leitura e escrita em todos os documentos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
