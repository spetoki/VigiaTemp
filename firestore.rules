rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regra de desenvolvimento: Permite leitura e escrita em todos os documentos.
    // AVISO: Inseguro para produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
