
// A tela de bloqueio foi removida pois o aplicativo foi convertido para o modo de demonstração.
// A autenticação por chave de acesso dependia da integração com o banco de dados Firebase.
export default function LockScreen() {
  return null;
}
